# Backend API URL (required in production — set to your deployed API endpoint)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Google OAuth client ID (required for Google Sign-In)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Set to "true" to disable Hot Module Replacement (AI Studio environments)
# DISABLE_HMR=true
